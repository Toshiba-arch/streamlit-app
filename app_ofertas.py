import streamlit as st
from PIL import Image, ImageDraw, ImageFont
import requests
from io import BytesIO

# Funções e código principal para o Gerador de Ofertas
def criar_imagem_com_texto(imagem_url, nome_produto, preco_original, preco_atual, desconto, tags, cupom=None):
    # Código relacionado ao gerador de imagem com desconto
    # ...

st.title("Gerador de Conteúdo com Ofertas da Amazon")
st.sidebar.header("Menu")
st.sidebar.subheader("Gerar Post")

# Código relacionado à interface de geração de ofertas
# ...
